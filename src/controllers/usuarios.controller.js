export const crearUsuario = (request, response) => {
  response.json({
    ok: true,
    msg: "Bienvenido a las notas",
  });
};

export const obtenerUsuarios = (request, response) => {
  response.json({
    ok: true,
    msg: "Bienvenido a las notas",
  });
};

export const actualizarUsuario = (request, response) => {
  response.json({
    ok: true,
    msg: "Bienvenido a las notas",
  });
};

export const eliminarUsuario = (request, response) => {
  response.json({
    ok: true,
    msg: "Bienvenido a las notas",
  });
};
